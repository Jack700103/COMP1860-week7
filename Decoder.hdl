CHIP Decoder {
    IN in[4];
    OUT a,b,c,d,e,f,g;
    PARTS:
    DecoderSegment(in=in, c=c);
    DecoderSegment(in=in, d=d);
    DecoderSegment(in=in, e=e);
    DecoderSegment(in=in, f=f);
    DecoderSegment(in=in, g=g);
    DecoderSegment(in=in, a=a);
    DecoderSegment(in=in, b=b);
}

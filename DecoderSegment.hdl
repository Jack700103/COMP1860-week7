CHIP DecoderSegment {
    IN in[4];
    OUT c;
    
    PARTS:

    Not(in=in[3], out=not3);
    Not(in=in[2], out=not2);
    Not(in=in[1], out=not1);
    Not(in=in[0], out=not0);

    Or(a=not0, b=not1, out=term1);
    And(a=in[2], b=not3, out=term2);
    Or(a=term1, b=term2, out=c);
}
